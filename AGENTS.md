# Agent Instructions

This file is read by AI agents working in this repository.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`HemantJangid/prompt-to-app`). Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary — `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` at the root plus `docs/adr/` for architectural decisions. See `docs/agents/domain.md`.
